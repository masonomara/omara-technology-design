# Post-publish review — {cycle}

Generated {stamp}. **{tldr}.**

Re-baselined the live site after this PR went live, against the cycle's pre-publish baseline (`01-baseline.md`, everything up to the PR).

> Read each row by its latency. Crawl counts (alt text, headings, meta) and lab a11y and SEO scores show the change now. Field and CrUX run on a 28-day rolling average. A just-published change won't move them yet. That lands in a later cycle, not in this comment.

▲ better · ▼ worse · only rows that changed are shown.

<!-- tier: field -->
## Field data — CrUX (28-day, the signal to trust)
Real-user data. A just-published change lands here in a later cycle, not now.

<!-- tier: lab -->
## Lab scores — Lighthouse §A
PSI lab swings about ±10 even at median-of-3. Treat small moves as noise.

<!-- tier: axe -->
## Accessibility — axe failing nodes (§D, golden path)

<!-- tier: counts -->
## Site-wide counts — §F (whole-site crawl)
